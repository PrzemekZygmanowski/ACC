import React from "react";
import { Header as HeaderComponent } from "../containers";

const Header = () => {
  return (
    <header>
      <HeaderComponent />
    </header>
  );
};

export default Header;
