import React from 'react';
import { Header as HeaderComponent } from '../domain';

const Header = () => {
  return (
    <>
      <HeaderComponent />
    </>
  );
};

export default Header;
